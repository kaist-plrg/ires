const Node = require('../Node');

let AsyncFunctionDeclaration = (Yield, Await, Default) => (given) => {
  Yield, Await, Default, given;
  Node.TODO('AsyncFunctionDeclaration');
}

module.exports = AsyncFunctionDeclaration;
